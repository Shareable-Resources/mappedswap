const express = require('express');
const router = express.Router();

const botController = require('../controllers/bot');

//router.post('/ticket/:id', botController.createMessage);

module.exports = router;